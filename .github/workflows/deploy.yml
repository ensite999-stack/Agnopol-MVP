name: Agnopol V2 ZK-Deployer

on:
  push:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      # 第一步：安装 ZK 编译环境（这是超越 Zcash 的数学引擎）
      - name: Install ZK Tools
        run: |
          npm install -g snarkjs
          # 使用预编译好的 circom 节省 Actions 时间
          sudo wget -qO /usr/local/bin/circom https://github.com/iden3/circom/releases/latest/download/circom-linux-amd64
          sudo chmod +x /usr/local/bin/circom

      # 第二步：编译 ZK 电路并生成 Solidity 验证者合约
      - name: Compile ZK Circuit
        run: |
          cd circuits
          circom Agnopol.circom --r1cs --wasm --sym
          # 生成临时开发用的 Power of Tau (正式版需进行仪式，基建初期先跑通逻辑)
          snarkjs powersoftau new bn128 12 pot12_0000.ptau -v
          snarkjs powersoftau contribute pot12_0000.ptau pot12_0001.ptau --name="First contribution" -v -e="some random text"
          snarkjs powersoftau prepare phase2 pot12_0001.ptau pot12_final.ptau -v
          snarkjs groth16 setup Agnopol.r1cs pot12_final.ptau Agnopol_0000.zkey
          # 关键点：将数学公式导出为 Solidity 代码
          snarkjs zkey export solidityverifier Agnopol_0000.zkey ../contracts/Verifier.sol

      # 第三步：安装依赖并部署到 Base
      - name: Install Dependencies
        run: npm install

      - name: Deploy to Base Sepolia
        env:
          PRIVATE_KEY: ${{ secrets.PRIVATE_KEY }}
          RPC_URL: "https://sepolia.base.org"
        run: npx hardhat run scripts/deploy.js --network base-sepolia
